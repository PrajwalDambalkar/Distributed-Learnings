<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        /* SJSU Blue Theme */
        :root {
            --sjsu-blue: #1e3a8a;
            --sjsu-light-blue: #3b82f6;
            --sjsu-gold: #fbbf24;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        /* Hero Section with SJSU CIDS Background */
        .hero-section {
            min-height: 100vh;
            background: linear-gradient(135deg, rgba(30, 58, 138, 0.5) 0%, rgba(59, 130, 246, 0.4) 100%),
                url('https://www.sjsu.edu/information-data-society/pics/cids-home-hero-092024.png');
            background-size: cover; /* 100% auto; */
            /* cover; */
            background-position: center center;
            background-repeat: no-repeat;
            background-attachment: scroll;
            color: white;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        /* Add overlay for better text readability */
        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(30, 58, 138, 0.3) 0%, rgba(59, 130, 246, 0.2) 100%);
            z-index: 1;
        }

        .hero-section .container {
            position: relative;
            z-index: 2;
        }

        /* Navigation */
        .navbar-sjsu {
            background: rgba(30, 58, 138, 0.95) !important;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.4rem;
        }

        /* Content Cards */
        .content-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .content-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
        }

        /* Buttons */
        .btn-sjsu-primary {
            background: var(--sjsu-blue);
            border: 2px solid var(--sjsu-blue);
            color: white;
            font-weight: 600;
            padding: 12px 30px;
            border-radius: 50px;
            transition: all 0.3s ease;
        }

        .btn-sjsu-primary:hover {
            background: var(--sjsu-light-blue);
            border-color: var(--sjsu-light-blue);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(30, 58, 138, 0.4);
        }

        .btn-sjsu-outline {
            background: transparent;
            border: 2px solid white;
            color: white;
            font-weight: 600;
            padding: 12px 30px;
            border-radius: 50px;
            transition: all 0.3s ease;
        }

        .btn-sjsu-outline:hover {
            background: white;
            color: var(--sjsu-blue);
            transform: translateY(-2px);
        }

        /* Alert Styles */
        .alert-success-custom {
            background: linear-gradient(135deg, #10b981, #06d6a0);
            border: none;
            color: white;
            border-radius: 15px;
        }

        .alert-info-custom {
            background: linear-gradient(135deg, var(--sjsu-light-blue), #8b5cf6);
            border: none;
            color: white;
            border-radius: 15px;
        }

        /* Animated elements */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in-up {
            animation: fadeInUp 0.8s ease forwards;
        }

        .fade-in-delay-1 {
            animation-delay: 0.2s;
            opacity: 0;
        }

        .fade-in-delay-2 {
            animation-delay: 0.4s;
            opacity: 0;
        }

        .fade-in-delay-3 {
            animation-delay: 0.6s;
            opacity: 0;
        }

        /* Footer */
        .footer-info {
            background: rgba(30, 58, 138, 0.1);
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            margin-top: 50px;
        }
    </style>
</head>

<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-sjsu fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-mortarboard-fill me-2"></i>
                <strong>ADS-SJSU</strong> Authentication System
            </a>
            <div class="navbar-nav ms-auto">
                <% if (user) { %>
                    <!-- User is logged in -->
                    <span class="navbar-text me-3">
                        <i class="bi bi-person-circle me-1"></i>
                        Welcome, <strong>
                            <%= user.name %>
                        </strong>
                    </span>
                    <a class="nav-link" href="/dashboard">
                        <i class="bi bi-speedometer2 me-1"></i>Dashboard
                    </a>
                    <a class="nav-link" href="/auth/logout">
                        <i class="bi bi-box-arrow-right me-1"></i>Logout
                    </a>
                    <% } else { %>
                        <!-- User is NOT logged in -->
                        <a class="nav-link" href="/auth/login">
                            <i class="bi bi-box-arrow-in-right me-1"></i>Login
                        </a>
                        <% } %>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <div class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-8 mx-auto text-center">
                    <!-- Main Welcome Content -->
                    <div class="content-card p-5 fade-in-up">
                        <div class="mb-4">
                            <h1 class="display-4 fw-bold text-primary mb-3">
                                Welcome to ADS-SJSU
                            </h1>
                            <p class="lead text-muted mb-4">
                                Department of Applied Data Science<br>
                                <strong>San José State University</strong>
                            </p>
                            <!-- <div class="text-muted">
                                <i class="bi bi-geo-alt-fill me-1"></i>
                                College of Information, Data and Society
                            </div> -->
                        </div>

                        <% if (user) { %>
                            <!-- Logged in user content -->
                            <div class="alert alert-success-custom fade-in-delay-1 fade-in-up" role="alert">
                                <div class="d-flex align-items-center justify-content-center">
                                    <i class="bi bi-check-circle-fill me-3 fs-3"></i>
                                    <div>
                                        <h4 class="alert-heading mb-2">Hello, <%= user.name %>! 👋</h4>
                                        <p class="mb-1">You are successfully authenticated as <strong>
                                                <%= user.username %>
                                            </strong></p>
                                        <small>Role: <span class="badge bg-light text-dark px-3">
                                                <%= user.role.toUpperCase() %>
                                            </span></small>
                                    </div>
                                </div>
                            </div>
                            <div class="d-grid gap-3 d-md-flex justify-content-center fade-in-delay-2 fade-in-up">
                                <a href="/dashboard" class="btn btn-sjsu-primary btn-lg">
                                    <i class="bi bi-speedometer2 me-2"></i>
                                    Access Dashboard
                                </a>
                                <a href="/auth/logout" class="btn btn-outline-secondary btn-lg">
                                    <i class="bi bi-box-arrow-right me-2"></i>
                                    Logout
                                </a>
                            </div>
                            <% } else { %>
                                <!-- Not logged in content -->
                                <div class="alert alert-info-custom fade-in-delay-1 fade-in-up" role="alert">
                                    <div class="d-flex align-items-center justify-content-center">
                                        <i class="bi bi-shield-lock-fill me-3 fs-3"></i>
                                        <div>
                                            <h4 class="alert-heading mb-2">Secure Access Portal</h4>
                                            <p class="mb-0">Please authenticate to access your dashboard and course
                                                materials</p>
                                        </div>
                                    </div>
                                </div>
                                <div class="d-grid gap-3 fade-in-delay-2 fade-in-up">
                                    <a href="/auth/login" class="btn btn-sjsu-primary btn-lg">
                                        <i class="bi bi-box-arrow-in-right me-2"></i>
                                        Login to Continue
                                    </a>
                                </div>
                                <% } %>

                                    <% if (typeof error !=='undefined' && error) { %>
                                        <!-- Error message if any -->
                                        <div class="alert alert-warning mt-4 fade-in-delay-3 fade-in-up" role="alert">
                                            <i class="bi bi-exclamation-triangle me-2"></i>
                                            <%= error %>
                                        </div>
                                        <% } %>
                    </div>

                    <!-- System Info Card
                    <div class="content-card mt-4 p-4 fade-in-delay-3 fade-in-up">
                        <div class="row align-items-center text-muted">
                            <div class="col-12 text-center">
                                <h6 class="mb-2 text-primary">
                                    <i class="bi bi-info-circle me-2"></i>
                                    About This System
                                </h6>
                                <p class="mb-0 small">
                                    Secure Node.js authentication system built with Express.js, EJS templates,
                                    and Bootstrap for DATA236: Distributed Systems for Data Engineering
                                </p>
                            </div>
                        </div>
                    </div> -->
                </div>
            </div>
        </div>

        <!-- Footer Info
        <div class="footer-info">
            <div class="container">
                <div class="row py-4">
                    <div class="col-12 text-center">
                        <small class="text-white-50">
                            <i class="bi bi-code-slash me-1"></i>
                            Features: Session Management • Protected Routes • Responsive Design • Bootstrap UI
                        </small>
                    </div>
                </div>
            </div>
        </div> -->
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>